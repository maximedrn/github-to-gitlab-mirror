linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - goimports
linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: github.com/martmull/github-to-gitlab-mirror
run:
  timeout: 3m
