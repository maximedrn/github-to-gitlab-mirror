pre-commit:
  parallel: true
  commands:
    golangci-lint:
      glob: "*.go"
      run: golangci-lint run ./...
    cspell:
      glob: "*.{go,md,yml,yaml,json}"
      run: cspell --no-progress --no-summary {staged_files}
